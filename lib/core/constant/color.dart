import 'package:flutter/material.dart';

class AppColor {
  // الألوان الأساسية المستخرجة من التصميمات

  // اللون الأساسي - بنفسجي متوسط إلى داكن (يظهر في العناوين والأزرار الرئيسية)
  static const Color primaryColor = Color(0xFF6B46C1);

  // اللون الثانوي - أزرق سماوي (يظهر في خلفية Analytics وحالات In Progress)
  static const Color secondaryColor = Color(0xFF0EA5E9);

  // لون الخلفية - أبيض مائل للرمادي الفاتح جداً
  static const Color backgroundColor = Color(0xFFF8FAFC);

  // لون السطح - أبيض نقي للبطاقات
  static const Color surfaceColor = Color(0xFFFFFFFF);

  // لون الخطأ - أحمر
  static const Color errorColor = Color(0xFFEF4444);

  // لون النجاح - أخضر (للحالات المكتملة)
  static const Color successColor = Color(0xFF10B981);

  // لون التحذير - برتقالي (للحالات المعلقة)
  static const Color warningColor = Color(0xFFF59E0B);

  // لون النص الأساسي - رمادي داكن جداً
  static const Color textColor = Color(0xFF1F2937);

  // لون النص الثانوي - رمادي متوسط
  static const Color textSecondaryColor = Color(0xFF6B7280);

  // لون الحدود - رمادي فاتح جداً
  static const Color borderColor = Color(0xFFE5E7EB);

  // لون الظل - أسود شفاف
  static const Color shadowColor = Color(0x1A000000);

  // ألوان إضافية من التصميمات

  //gradient colors
  static const Color gradientStart = Color(0xFF9333EA);
  static const Color gradientMiddle = Color(0xFF3B82F6);
  ////

  static const Color gradientEnd = Color(0xFF60A5FA);

  // لون البطاقات النشطة - بنفسجي فاتح
  static const Color activeCardColor = Color(0xFF8B5CF6);

  // لون البطاقات غير النشطة - رمادي فاتح
  static const Color inactiveCardColor = Color(0xFFF3F4F6);

  // لون النص على الخلفيات المظلمة
  static const Color textOnDark = Color(0xFFFFFFFF);

  // لون النص على الخلفيات الفاتحة
  static const Color textOnLight = Color(0xFF1F2937);

  // لون الخلفية المظلمة للوضع الليلي
  static const Color darkBackground = Color(0xFF111827);

  // لون السطح المظلم للوضع الليلي
  static const Color darkSurface = Color(0xFF1F2937);

  // لون الأبيض
  static const Color white = Color(0xFFFFFFFF);
}
