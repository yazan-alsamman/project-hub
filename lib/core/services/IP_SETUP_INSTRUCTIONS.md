# 🔧 إعداد IP Address للجهاز الحقيقي

## المشكلة الحالية

عند تجربة التطبيق على **جهاز حقيقي**، يظهر خطأ "No internet connection" حتى لو كان الجهاز متصل بالإنترنت.

## السبب

على الجهاز الحقيقي، لا يمكن استخدام:
- ❌ `localhost` 
- ❌ `10.0.2.2` (يعمل فقط على Android Emulator)

يجب استخدام **IP Address الفعلي** للكمبيوتر الذي يعمل عليه الـ server.

---

## ✅ الحل السريع

### الخطوة 1: معرفة IP Address

#### على Windows:
```cmd
ipconfig
```
ابحث عن **IPv4 Address** تحت:
- **Wireless LAN adapter Wi-Fi** (إذا كنت متصل بـ WiFi)
- **Ethernet adapter** (إذا كنت متصل بكابل)

مثال: `192.168.1.100`

#### على Mac/Linux:
```bash
ifconfig
```
ابحث عن **inet** تحت `en0` أو `wlan0`

مثال: `192.168.1.100`

---

### الخطوة 2: تحديث Base URL

#### الطريقة السريعة: تحديث في الكود

1. افتح ملف: `lib/core/constant/api_constant.dart`
2. ابحث عن السطر:
   ```dart
   static const String _customBaseUrl = ''; // ضع IP address هنا للجهاز الحقيقي
   ```
3. ضع IP address الخاص بك:
   ```dart
   static const String _customBaseUrl = 'http://192.168.1.100:5000'; // استبدل بـ IP address الخاص بك
   ```

#### مثال:
إذا كان IP address هو `192.168.1.100`:
```dart
static const String _customBaseUrl = 'http://192.168.1.100:5000';
```

---

### الخطوة 3: التأكد من نفس الشبكة

**مهم جداً:** يجب أن يكون:
- ✅ الكمبيوتر (الذي يعمل عليه الـ server) متصل بـ WiFi
- ✅ الجهاز الحقيقي متصل **بنفس شبكة WiFi**
- ✅ الـ server يعمل على المنفذ 5000

---

### الخطوة 4: اختبار الاتصال

#### من المتصفح على الجهاز الحقيقي:
افتح المتصفح وجرب:
```
http://YOUR_IP_ADDRESS:5000/user/login
```

إذا ظهرت صفحة أو خطأ من الـ server (مثل "Method not allowed" أو JSON response)، فالاتصال يعمل ✅

---

## 🔍 حلول للمشاكل الشائعة

### المشكلة: "No internet connection"
**الحل:**
1. تأكد من أنك وضعت IP address الصحيح
2. تأكد أن الكمبيوتر والجهاز على نفس الشبكة
3. تحقق من أن الـ server يعمل على المنفذ 5000

### المشكلة: "Connection refused"
**الحل:**
1. تحقق من Windows Firewall - قد يحتاج لفتح المنفذ 5000
2. تأكد أن الـ server يستمع على `0.0.0.0` وليس `127.0.0.1`
3. جرب الوصول من المتصفح أولاً

### المشكلة: IP Address يتغير
**الحل:**
- استخدم IP address ثابت في الـ router
- أو ضع IP address في إعدادات التطبيق

---

## 📝 ملاحظات مهمة

1. **IP Address يتغير:** كل مرة تغير فيها الشبكة (WiFi)، قد يتغير IP address
2. **نفس الشبكة:** يجب أن يكون الكمبيوتر والجهاز على نفس WiFi
3. **Firewall:** قد تحتاج لفتح المنفذ 5000 في Windows Firewall

---

## 🚀 بعد التحديث

1. احفظ الملف
2. أعد تشغيل التطبيق (Hot Restart)
3. جرب تسجيل الدخول مرة أخرى

---

## 📞 للدعم

إذا استمرت المشكلة:
1. تحقق من Console logs - سترى الـ URL المستخدم
2. تأكد من IP address الصحيح
3. اختبر الاتصال من المتصفح أولاً

